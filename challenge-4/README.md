# Challenge 4

Being on MacOs i had to create an emulator to make sure the script is working as expected on UTM

Once working a simple strings blackbox showed a reference to a file `the_magic_filez.txt`, I was lucky enough to make it work by creating this file next to the binary to make it work

another simple way to find out is using strace (easy) or objdump (more detailed)
